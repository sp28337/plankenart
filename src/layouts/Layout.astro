---
import BaseLayout from './BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import FormModal from '../components/ui/FormModal.astro';

interface Props {
  pageTitle: string;
  pageDescription: string;
  ogImg?: string;
}

const { 
  pageTitle,
  pageDescription,
  ogImg,
} = Astro.props;
---
<BaseLayout
  pageTitle={pageTitle}
  pageDescription={pageDescription}
  ogImg={ogImg}
>
	<Header/>
	<main class="w-full" id="top">
    <slot />
	</main>
  <Footer/>
  <FormModal />
</BaseLayout>
<script>
  import "@features/animations/reveal"
</script>